name: AI Auto-Fix Test
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing if GitHub Actions works"
      - run: echo "If this passes, we can add AI"
